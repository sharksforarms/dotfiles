# dotfiles

My dotfiles. Ansible is used to provision, and stow is used to symlink directories

# Requirements

```
apt install ansible
```

# Provision

`./bootstrap`

To run specific,

`./bootstrap --tags apt`

